/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
      defaultLocale: 'es', // Establece el idioma predeterminado
      locales: ['en', 'es'], // Idiomas que tu aplicación soportará
    },
  };
  
  export default nextConfig;
  